<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ostrich app</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;

        }

        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-width: 100vh;
            padding: 4rem;
        }

        .container {
            min-height: 10rem;
            min-width: 20rem;
            box-shadow: 0 0 2rem -1rem #0000006b,0 0 2rem -1rem #0000006b;
            display: flex;
            flex-direction: column;
            padding: 2rem;
            border-radius: 20px;
            text-align: center;
            gap: 10px;
            font-family: Poppins, Roboto, Raleway, Arial, Helvetica, sans-serif, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            justify-content: center;
        }
        h2{
            font-size: 2rem;
            text-decoration: underline;
        }
        code{
            background-color: #000000fc;
            color: #ffffff;
            border-radius: 10px ;
            padding: 2px 10px;
        }
        .doc-def{
            text-align: left;
            font-weight: 500;
            font-size: 1rem;
        }
    </style>
</head>

<body>
    <div class="container">

        <% if (data) { %>
            <h2>Welcome to <%= data.appName %> app</h2>
            <p class="doc-def">Api base <code>/api/v1/</code></p>
            <p class="doc-def">Api port <code><%= data.port %></code></p>
            <p class="doc-def">Api name <code><%= data.appName %></code></p>
            <p class="doc-def">Api docs <code>http://localhost:<%= data.port %>/api/v1/docs</code></p>
            <p class="doc-def">All the documentation are in the docs route <br/><a href="http://localhost:<%= data.port %>/api/v1/docs">Ostrich docs</a>
            </p>
            <% } %>
</body>
</div>

</html>
